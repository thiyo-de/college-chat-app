| Step | Feature                         | Description                                         |
| ---- | ------------------------------- | --------------------------------------------------- |
| 1️⃣  | Vite + React Setup              | Already done — working project and socket connected |
| 2️⃣  | Socket Client (Separate File)   | Use `services/socket.js` to handle all socket logic |
| 3️⃣  | Chat Input & Send Button        | Let users type and send messages via Enter or Send  |
| 4️⃣  | Display Public Messages         | Messages are shown in a chat bubble list            |
| 5️⃣  | File Upload (Image/PDF Preview) | Let users send image/PDF and preview before sending |
| 6️⃣  | Auto Scroll, Time Format        | Smooth scroll to latest message, format timestamps  |
| 7️⃣  | Optional: User Name/Join Info   | Show user who sent what, who joined, etc.           |

1. ✅ Build Auth
   - Register
   - Login
   - JWT/Token storage
   - Route protection (optional)

2. ✅ Setup Global User Context (optional but good)
   - Store user info from token or API
   - Access `user._id` across app

3. 🚀 Build Core Features (Chat, Upload, Admin Tools, etc.)
   - Now you can use `user._id` to store messages, protect routes, etc.

4. 🔒 Add Auth-Based Route Guards
   - e.g. Redirect to `/login` if no token

